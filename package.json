{
  "name": "ogx-systems",
  "version": "1.0.0",
  "description": "SYSTEMS.OS baseline runtime for OGX Systems",
  "main": "worker.js",
  "type": "commonjs",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "validate:json": "node scripts/validate-json.js",
    "validate:yaml": "node scripts/validate-yaml.js",
    "validate": "npm run validate:json && npm run validate:yaml"
  },
  "devDependencies": {
    "yaml": "^2.9.0"
  }
}
